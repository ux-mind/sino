import React, { useEffect } from "react";
import { styled, connect } from "frontity";

const Test = ({ state, actions, post }) => {
  return (
    'TEST COMPONENT'
  );
};

export default connect(Test);
